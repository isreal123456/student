# from django.urls import path
# from book.views import HomePage, createbook, listbook,detail
#
# urlpatterns = [
#     path("", HomePage.as_view(), name="home"),
#     path("book/create/", createbook.as_view(), name="create"),
#     # path("book/lists", listbook.as_view(), name="lists"),
#     path("book/detail/<int:pk>", detail.as_view(), name="detail")
# ]